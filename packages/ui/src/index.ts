/**
 * @orghub/ui — Shared React component library
 *
 * Components here are org-agnostic and use CSS variables for theming
 * (--org-primary, --org-secondary) so they adapt to any org's branding.
 */

export { Badge } from "./Badge";
export { Button } from "./Button";
export { Card } from "./Card";
export { EmptyState } from "./EmptyState";
export { Spinner } from "./Spinner";
