# Supabase (single shared project for all orgs)
NEXT_PUBLIC_SUPABASE_URL=https://fvuqugtzfgeqzshrezqw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Routing
NEXT_PUBLIC_ROOT_DOMAIN=orghub.app

# Email
SENDGRID_API_KEY=SG.your-key
DEFAULT_FROM_EMAIL=hello@orghub.app

# Zoom Meeting SDK (optional, per-org feature)
ZOOM_MEETING_SDK_CLIENT_ID=
ZOOM_MEETING_SDK_CLIENT_SECRET=

# Cron Job Security
CRON_SECRET=your-random-secret-here-change-in-production

# Stripe keys are stored per-org in the organizations table (via Settings page).
# No global Stripe keys needed here.
